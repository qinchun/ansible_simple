- name: Jenkins
  hosts: jenkins
  tasks:
    - name: Restart Jenkins
      sudo: false
      command: java -jar {{ jenkins.cli_dest }} -s http://localhost:8080 safe-restart
  #roles:
  #  - ansible-jenkins
   
  
